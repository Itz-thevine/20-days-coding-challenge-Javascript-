// Define a function which can generate a dictionary where the keys are numbers between 1 and
// 20 (both included) and 
// the values are square of keys. The function should just print the keys only.

dictionary();
var dict;

function dictionary () {
    for (let i = 1; i <= 20; i++) {
        var dict = {
            i : i**2
        }
        console.log(Math.sqrt(dict.i))
    }
   
}



// console.log('hello');